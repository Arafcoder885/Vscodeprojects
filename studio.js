/* ═══════════════════════════════════════════════════════════
   LESSON 1 — Structuring the Interface
   GOAL: Wire up the form UI — chips, color pickers, button.
   ═══════════════════════════════════════════════════════════ */

(function () {
  'use strict';

  // TODO 1: Get references to DOM elements
  // generateBtn, formError, emptyState, result


  // TODO 2: Wire up chip selection
  // For each .chip-group: on chip click, remove 'active' from
  // all chips, add 'active' to clicked chip, update hidden input


  // TODO 3: Write syncColor(pickerId, textId)
  // On picker input: update text field
  // On text input: if valid 6-digit hex, update picker
  // Call for 'primary_color'/'primary_color_text' and 'accent_color'/'accent_color_text'


  // TODO 4: Wire Generate button — show placeholder result
  // On click: hide emptyState, show result panel with
  // placeholder values (style, material, colors) to confirm UI works

})();
